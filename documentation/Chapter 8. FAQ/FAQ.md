# **8. Часто задаваемые вопросы (FAQ по проекту)**

## **8.1. Как добавить изображение к статье?**

### **Для публикаций (статей)**

1. **Создайте папку для изображений статьи:**
    - Перейдите: `public/images/publications/`
    - Создайте папку с названием статьи на транслите (например, `novaya-statya`)

2. **Загрузите изображения:**
    - В созданную папку загрузите все нужные изображения
    - Рекомендуемые форматы: JPG (для фото), PNG (для графиков)
    - Оптимальный размер: 1200-2000px по ширине, до 500KB

3. **Добавьте изображение в статью:**
    - В Front Matter укажите обложку:
      ```markdown
      ---
      titlePhoto: '/images/publications/novaya-statya/cover.jpg'
      ---
      ```
    - В тексте статьи вставляйте изображения:
      ```markdown
      ![Описание изображения](/images/publications/novaya-statya/image1.jpg)
      
      ![График результатов](/images/publications/novaya-statya/chart.png "Результаты исследования")
      ```

### **Для новостей**

1. **Загрузите изображение в папку новостей:**
    - Перейдите: `public/images/news/`
    - Загрузите изображение с описательным именем (например, `novyi-kabinet.jpg`)

2. **Используйте в новости:**
   ```markdown
   ---
   image: '/images/news/novyi-kabinet.jpg'
   ---
   
   # Заголовок новости
   
   ![Фото нового кабинета](/images/news/novyi-kabinet.jpg)
   ```

### **Общие рекомендации:**
- Используйте осмысленные имена файлов на латинице
- Оптимизируйте изображения перед загрузкой (можно использовать tinypng.com)
- Для одного изображения в статье можно использовать путь прямо в `public/images/`

## **8.2. Как вставить таблицу в Markdown?**

### **Базовый синтаксис таблиц:**

```markdown
| Заголовок 1 | Заголовок 2 | Заголовок 3 |
|-------------|-------------|-------------|
| Ячейка 1    | Ячейка 2    | Ячейка 3    |
| Ячейка 4    | Ячейка 5    | Ячейка 6    |
```

### **Пример для прайс-листа:**

```markdown
| Услуга | Длительность | Цена |
|--------|--------------|------|
| Консультация врача | 60 мин | 3 000 ₽ |
| Массаж | 45 мин | 2 500 ₽ |
| ЛФК | 60 мин | 3 500 ₽ |
```

### **С выравниванием столбцов:**

```markdown
| Услуга | Длительность | Цена |
|:-------|:------------:|-----:|
| Консультация | 60 мин | 3000 ₽ |
| Массаж | 45 мин | 2500 ₽ |
```
- `:---` - выравнивание по левому краю
- `:---:` - выравнивание по центру
- `---:` - выравнивание по правому краю

### **Таблица с объединенными ячейками (расширенный синтаксис):**

```markdown
| День | Утро | Вечер |
|------|------|-------|
| Понедельник | 9:00 - 13:00 | 14:00 - 20:00 |
| Вторник | 9:00 - 13:00 | 14:00 - 20:00 |
| Суббота | 10:00 - 14:00 | - |
| Воскресенье | Выходной | Выходной |
```

## **8.3. Деплой не сработал – где искать ошибки?**

### **Шаг 1: Проверьте вкладку Actions**

1. Перейдите: `https://github.com/klinika-zdorovya/web/actions`
2. Найдите последний workflow с красным крестиком 🔴
3. Нажмите на него для просмотра деталей

### **Шаг 2: Анализ логов ошибок**

**Распространенные ошибки и решения:**

#### **Ошибка 1: Синтаксическая ошибка в YAML**
```
Error: YAMLException: bad indentation of a mapping entry at line 5
```
**Решение:**
- Проверьте отступы в YAML файлах (используйте пробелы, а не табуляцию)
- Убедитесь, что все отступы одинаковые (обычно 2 пробела)

#### **Ошибка 2: Неправильный JSON**
```
Error: Unexpected token in JSON at position 42
```
**Решение:**
- Проверьте JSON файлы на наличие лишних или недостающих запятых, кавычек
- Используйте валидатор JSON (можно найти онлайн)

#### **Ошибка 3: Изображение не найдено**
```
Error: Image not found: /images/doctors/novikov.png
```
**Решение:**
- Проверьте, что файл действительно существует в `public/images/doctors/`
- Убедитесь, что имя файла написано правильно (регистр важен)

#### **Ошибка 4: Проблемы с FTP**
```
Error: Timeout (control socket)
```
**Решение:**
1. Проверьте секреты FTP в настройках репозитория
2. Убедитесь, что FTP сервер доступен
3. Проверьте, не изменился ли пароль FTP

#### **Ошибка 5: Ошибка сборки Nuxt**
```
Error: Cannot find module '@nuxt/content'
```
**Решение:**
- Удалите `node_modules` и `package-lock.json`
- Запустите `npm install` локально для проверки
- Если ошибка осталась, проверьте `package.json` на наличие всех зависимостей

### **Шаг 3: Исправление и повторный запуск**

1. Исправьте ошибку в соответствующем файле
2. Сделайте коммит с исправлениями
3. Дождитесь нового запуска workflow

### **Экстренные меры:**

Если сайт сломан и нужно быстро восстановить:
1. Найдите последний рабочий коммит в истории
2. Сделайте revert этого коммита
3. Или восстановите файлы из истории Git

## **8.4. Как изменить порядок вывода элементов в списке (например, новостей)?**

### **Для новостей и отзывов:**

**По дате (по умолчанию):**
- Новые элементы показываются первыми
- Сортировка по полю `date` в порядке убывания

**Как изменить дату:**
```markdown
---
date: '2024-12-25'  # Измените на нужную дату
---
```

### **Для врачей:**

**Порядок определяется номером в имени файла:**
- `1.rodichkin.json` → первый в списке
- `2.pupkov.json` → второй в списке
- `3.slyazin.json` → третий в списке

**Как изменить порядок:**
1. Переименуйте файлы, изменив номер в начале
2. Например, чтобы сделать врача первым: `1.ivanov.json`

### **Для продуктов (ортопедической продукции):**

**Аналогично врачам:**
- `1.matras.md` → первый продукт
- `2.pillow.md` → второй продукт

**Изменение порядка:**
```bash
# Переименовать файлы через GitHub:
1. Нажмите на файл
2. Нажмите "Rename"
3. Измените номер в начале имени
```

### **Для FAQ (вопросов-ответов):**

**Порядок по номеру в имени файла:**
- `1.question.json` → первый вопрос
- `2.question.json` → второй вопрос

### **Для публикаций (статей):**

**Сортировка по дате публикации:**
```markdown
---
date: '2015-10-30'  # Более старая дата - ниже в списке
---
```

## **8.5. Как обновить информацию в шапке или подвале сайта?**

### **Шапка сайта (хедер):**

**Навигационное меню:**
- Редактируется в `content/data/navigation/navigation.yaml`
- Изменения вступят в силу после деплоя

**Логотип:**
- Находится в `public/images/logo/`
- Для замены загрузите новый файл с тем же именем
- Или обновите путь в компоненте шапки

### **Подвал сайта (футер):**

**Контактная информация:**
- Редактируется в `content/data/contacts/contacts.yaml`
- Изменяйте адрес, телефоны, график работы

**Социальные сети:**
- Если есть блок соцсетей, он настраивается в `contacts.yaml`
- Или в отдельном файле настроек футера

**Копирайт и дополнительная информация:**
- Обычно находится в компоненте футера
- Файл: `app/components/MainFooter.vue`
- Для изменения нужно редактировать Vue-компонент

### **Быстрое обновление телефона:**

1. Откройте `content/data/contacts/contacts.yaml`
2. Найдите секцию `phones`
3. Измените номер:
   ```yaml
   phones:
     - description: "Для записи к врачу"
       phone: "+79991234567"  # Новый номер
   ```

### **Обновление графика работы:**

```yaml
workingTime: "понедельник – пятница с 9:00 до 20:00, суббота с 10:00 до 18:00"
holidaysTime: "Воскресенье – выходной день"
```

## **8.6. Как изменить цветовую тему сайта?**

### **Светлая/темная тема:**

**Тема переключается автоматически** или через переключатель в интерфейсе.

**Настройка цветов в Tailwind:**
Все цвета определены в `tailwind.config.js`:

```javascript
// Пример изменения основного цвета:
colors: {
  'brand-light': '#174f54',  // Измените этот цвет
  // ...
}
```

### **Что нужно изменить:**

1. **Основные цвета:**
    - `brand-light` - основной брендовый цвет (заголовки, акценты)
    - `background` - цвет фона
    - `background-content` - фон контентных блоков

2. **Для светлой темы** меняйте цвета без суффикса `__dark`
3. **Для темной темы** меняйте цвета с суффиксом `__dark`

### **Пример изменения основного цвета:**

1. Откройте `tailwind.config.js`
2. Найдите нужный цвет:
   ```javascript
   'brand-light': '#174f54',  // Текущий цвет
   ```
3. Измените на новый:
   ```javascript
   'brand-light': '#2C5282',  // Новый синий цвет
   ```
4. Сохраните и запустите деплой

### **Важные замечания:**
- После изменения конфига нужно перезапустить dev сервер локально
- Для продакшена изменения применятся после деплоя
- Все цвета связаны между собой, меняйте аккуратно

## **8.7. Как добавить новый пункт в главное меню?**

### **Шаг 1: Добавление в навигацию**

1. Откройте `content/data/navigation/navigation.yaml`
2. Найдите массив `body`
3. Добавьте новый элемент:

```yaml
- title: 'Новый раздел'
  fullTitle: 'Полное название нового раздела'
  path: '/new-section/'
```

### **Шаг 2: Создание контента**

1. Создайте папку для раздела:
   ```bash
   content/new-section/
   ```

2. Создайте файл `index.md`:
   ```bash
   content/new-section/index.md
   ```

3. Заполните содержимое:
   ```markdown
   # Новый раздел
   
   Содержимое нового раздела...
   ```

### **Шаг 3: Добавление подпунктов (опционально)**

```yaml
- title: 'Новый раздел'
  fullTitle: 'Новый раздел'
  path: '/new-section/'
  children:
    - title: 'Подраздел 1'
      fullTitle: 'Подраздел 1'
      path: '/new-section/subsection1/'
    - title: 'Подраздел 2'
      fullTitle: 'Подраздел 2'
      path: '/new-section/subsection2/'
```

### **Шаг 4: Создание дочерних страниц**

```bash
mkdir -p content/new-section/subsection1
mkdir -p content/new-section/subsection2

touch content/new-section/subsection1/index.md
touch content/new-section/subsection2/index.md
```

### **Пример готовой структуры:**

```yaml
# navigation.yaml
body:
  # ... существующие разделы
  
  - title: Акции
    fullTitle: Акции и скидки
    path: '/akcii/'
    children:
      - title: Текущие акции
        fullTitle: Текущие акции
        path: '/akcii/current/'
      - title: Архив акций
        fullTitle: Архив акций
        path: '/akcii/archive/'
```

## **8.8. Как создать новую страницу с уникальным дизайном?**

### **Вариант 1: Использование существующих компонентов**

1. **Создайте Markdown файл** в нужной папке
2. **Используйте кастомные компоненты** в Markdown:

```markdown
# Страница с уникальным дизайном

## ::main-button
Кнопка с действием

Обычный текст...

## ::special-component{param="value"}
Кастомный компонент
```

### **Вариант 2: Создание новой Vue-страницы**

1. **Создайте файл страницы** в `app/pages/`:
   ```bash
   touch app/pages/custom-page.vue
   ```

2. **Реализуйте уникальный дизайн:**

```vue
<!-- app/pages/custom-page.vue -->
<template>
  <div class="custom-page">
    <!-- Уникальная верстка -->
    <div class="hero-section bg-brand-light text-white">
      <h1 class="heading-1">Уникальная страница</h1>
    </div>
    
    <!-- Использование существующих компонентов -->
    <DoctorList />
    
    <!-- Кастомный блок -->
    <div class="custom-block">
      <h2>Специальное предложение</h2>
      <!-- ... -->
    </div>
  </div>
</template>

<script setup>
// Импортируем нужные компоненты
import DoctorList from '~/components/Doctor/List.vue'

// Можно загружать данные
const { data: customData } = await useAsyncData('custom', () => 
  queryContent('custom-data').findOne()
)
</script>

<style scoped>
.custom-page {
  /* Уникальные стили для этой страницы */
}

.hero-section {
  padding: 5rem 0;
  text-align: center;
}

.custom-block {
  background: linear-gradient(135deg, var(--brand-light), var(--background-brand-green));
  padding: 3rem;
  border-radius: 1rem;
  margin: 3rem 0;
}
</style>
```

3. **Добавьте страницу в навигацию** (если нужно)

### **Вариант 3: Создание динамического маршрута**

1. **Используйте существующий динамический маршрут:**
    - Файл `app/pages/[...slug].vue` обрабатывает все страницы из content
    - Создайте файл в `content/` с нужным путем

2. **Создайте кастомный layout:**
   ```bash
   touch app/layouts/custom.vue
   ```

3. **Укажите layout в Front Matter:**
   ```markdown
   ---
   layout: 'custom'
   ---
   ```

## **8.9. Как работать с компонентами в Markdown?**

### **Встроенные компоненты:**

#### **Кнопки:**
```markdown
## ::main-button
Текст кнопки
```

#### **Стикеры:**
```markdown
::sticker{type="info"}
Важная информация
::
```

#### **Произвольные компоненты:**
```markdown
::doctor-card{name="Родичкин"}
::
```

### **Создание кастомного компонента для Markdown:**

1. **Создайте компонент:**
   ```bash
   touch app/components/Custom/Alert.vue
   ```

2. **Реализуйте компонент:**
   ```vue
   <!-- app/components/Custom/Alert.vue -->
   <template>
     <div :class="['alert', `alert-${type}`]">
       <Icon :name="iconName" class="w-5 h-5 mr-2" />
       <slot />
     </div>
   </template>

   <script setup>
   const props = defineProps({
     type: {
       type: String,
       default: 'info',
       validator: (value) => ['info', 'warning', 'success', 'error'].includes(value)
     }
   })

   const iconName = computed(() => {
     const icons = {
       info: 'material-symbols:info',
       warning: 'material-symbols:warning',
       success: 'material-symbols:check-circle',
       error: 'material-symbols:error'
     }
     return icons[props.type]
   })
   </script>

   <style scoped>
   .alert {
     @apply p-4 rounded-lg border flex items-start;
   }
   
   .alert-info {
     @apply bg-blue-50 border-blue-200 text-blue-800;
   }
   
   .alert-warning {
     @apply bg-yellow-50 border-yellow-200 text-yellow-800;
   }
   </style>
   ```

3. **Используйте в Markdown:**
   ```markdown
   ::alert{type="warning"}
   Это предупреждение для пациентов!
   ::
   
   ::alert{type="success"}
   Успешное завершение процедуры!
   ::
   ```

### **Компоненты с содержимым:**

```vue
<!-- app/components/Custom/Expandable.vue -->
<template>
  <details class="expandable">
    <summary class="cursor-pointer font-semibold p-2 bg-background-block rounded">
      {{ title }}
    </summary>
    <div class="p-4">
      <slot />
    </div>
  </details>
</template>

<script setup>
defineProps({
  title: {
    type: String,
    required: true
  }
})
</script>
```

```markdown
::expandable{title="Подробнее о процедуре"}
Здесь подробное описание процедуры...
::
```

## **8.10. Как настроить перенаправления (редиректы)?**

### **Вариант 1: На уровне Nuxt (рекомендуется)**

1. **Создайте файл редиректов:**
   ```bash
   touch app/plugins/redirects.client.js
   ```

2. **Реализуйте логику редиректов:**
   ```javascript
   // app/plugins/redirects.client.js
   export default defineNuxtPlugin(() => {
     const route = useRoute()
     
     // Таблица редиректов
     const redirects = {
       '/old-page': '/new-page',
       '/services/old-service': '/dimensions/physculture',
       '/blog': '/publications/list',
     }
     
     // Проверка текущего пути
     const currentPath = route.path
     
     if (redirects[currentPath]) {
       navigateTo(redirects[currentPath], { redirectCode: 301 })
     }
   })
   ```

### **Вариант 2: Через конфигурацию Nuxt**

1. **Добавьте в `nuxt.config.ts`:**
   ```typescript
   // nuxt.config.ts
   export default defineNuxtConfig({
     // ...
     routeRules: {
       '/old-page': { redirect: '/new-page' },
       '/services/**': { redirect: '/dimensions/**' },
     },
   })
   ```

### **Вариант 3: На уровне сервера (через .htaccess)**

Если хостинг поддерживает .htaccess:

1. **Создайте файл `public/.htaccess`:**
   ```
   RewriteEngine On
   
   # Редирект со старой страницы на новую
   Redirect 301 /old-page.html /new-page
   
   # Редирект всей папки
   RedirectMatch 301 ^/old-folder/(.*)$ /new-folder/$1
   
   # Редирект с параметрами
   RewriteCond %{QUERY_STRING} ^id=123$
   RewriteRule ^page\.php$ /new-page? [R=301,L]
   ```

### **Вариант 4: Через JavaScript на странице**

Для отдельных страниц:

```vue
<!-- app/pages/old-page.vue -->
<template>
  <div>
    <!-- Контент не показывается, сразу редирект -->
  </div>
</template>

<script setup>
onMounted(() => {
  // Постоянный редирект (301)
  window.location.replace('/new-page')
  
  // Или через navigateTo Nuxt
  navigateTo('/new-page', { redirectCode: 301 })
})
</script>
```

### **Примеры распространенных редиректов:**

1. **Редирект после переименования страницы:**
   ```javascript
   '/clinic/about-old': '/clinic/about'
   ```

2. **Редирект после изменения структуры:**
   ```javascript
   '/services/massage': '/dimensions/therapy'
   ```

3. **Редирект с параметрами:**
   ```javascript
   '/article.php?id=123': '/publications/some-article'
   ```

4. **Групповой редирект:**
   ```javascript
   '/news/2023/*': '/news/archive/*'
   ```

### **Проверка редиректов:**

1. **Локально:** Откройте старый URL в браузере
2. **На продакшене:** Используйте инструменты разработчика → Сеть
3. **Статус коды:** Убедитесь, что используется 301 (постоянный) или 302 (временный)

---

**В следующем разделе** добавим **Приложения** с шаблонами файлов, справочником по Markdown и ссылками на документацию.